FROM node:lts

WORKDIR /frontend
COPY ./package.json /frontend
COPY ./package-lock.json /frontend

RUN npm install -g npm && npm install --dev && npm install -g parcel-bundler

COPY . /frontend

CMD parcel build bundles-src/index.js -d bundles --public-url='./'